import React from 'react';

// Password change guard removed - no longer restricting users
const PasswordChangeGuard = ({ children }) => {
  return <>{children}</>;
};

export default PasswordChangeGuard;

